# For Ubuntu 18/20

# Install Apache
#   normal Apache installation and only a single additional module - WSGI (Python 3 version)
#   note- you can run VocPrez using a dedicated WSGI server, like gunicorn, and proxy to it form Apache instead
#   see VocPrez' Docker instructions (https://github.com/RDFLib/VocPrez/#running-in-docker)
sudo apt install -y apache2
sudo apt install -y libapache2-mod-wsgi-py3
sudo a2enmod wsgi

# configure Apache
sudo nano /etc/apache2/sites-available/000-default.conf
## insert apache.conf content
sudo service apache2 restart

# make VocPrez home dir
sudo mkdir /var/www/vocprez
sudo chown -R ubuntu /var/www/vocprez  # so we don't have to use sudo


# Get VocPrez
cd /var/www/vocprez
git clone https://github.com/RDFLib/VocPrez.git .
sudo chown -R www-data /var/www/vocprez/vocprez/cache  # so Apache can write cache files
# configure app.wsgi

# Install Python
sudo apt install python3-pip
sudo pip install venv
python -m venv venv  # making a virtual environment for Python for this application's packages
source venv/bin/activate

# Install required Python modules
pip install -r requirements.txt

# Customise
cd ~
git clone https://github.com/surroundaustralia/VocPrez-theme-gsq.git 
# configure VocPrez-theme-gsq/_config/__init__.py
# configure VOCPREZ_HOME variable in apply.py
python VocPrez-theme-cgi/apply.py
